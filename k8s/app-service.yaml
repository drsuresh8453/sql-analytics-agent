apiVersion: v1
kind: Service
metadata:
  name: sql-agent-service
  namespace: sql-analytics-agent
spec:
  selector:
    app: sql-agent
  ports:
    - name: api
      port: 8000
      targetPort: 8000
    - name: ui
      port: 8501
      targetPort: 8501
  type: ClusterIP
